import BootstrapButton from '../../bootstrap-button';
import layout from '../../../templates/components/bootstrap/-form-controls/edit-button';

export default BootstrapButton.extend({
  layout,
  classNames: ['btn-primary'],
});
