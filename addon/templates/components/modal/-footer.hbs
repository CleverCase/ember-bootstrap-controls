{{#if hasBlock}}
  {{yield}}
{{else}}
  {{#bootstrap-button
    classNames="btn-secondary"
    action=closeAction}}
    {{btnCloseLabel}}
  {{/bootstrap-button}}
  {{#bootstrap-button
    classNames="btn-primary"
    action=acceptAction}}
    {{btnAcceptLabel}}
  {{/bootstrap-button}}
{{/if}}
