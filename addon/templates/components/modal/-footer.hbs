{{#if hasBlock}}
  {{yield
    (hash
      acceptAction=acceptAction
      acceptButton=(component 'modal/footer/-accept-button' action=acceptAction)
      closeAction=closeAction
      closeButton=(component 'modal/footer/-close-button' action=closeAction)
    )
  }}
{{else}}
  {{modal/footer/-close-button action=closeAction}}
  {{modal/footer/-accept-button action=acceptAction}}
{{/if}}
