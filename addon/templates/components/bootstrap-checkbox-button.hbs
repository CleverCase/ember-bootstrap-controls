{{#if isChecked}}
  <input name='{{groupName}}' checked='true' type='checkbox' onclick={{action onChange option isNotChecked}}>
  {{yield option}}
{{else}}
  <input name='{{groupName}}' type='checkbox' onclick={{action onChange option isNotChecked}}>
  {{yield option}}
{{/if}}

