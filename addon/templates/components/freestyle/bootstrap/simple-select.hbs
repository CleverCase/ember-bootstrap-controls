{{#section.subsection name="Select Input"}}
  {{#freestyle-collection
    defaultKey="Basic Usage"
    inline=false
    as |collection|
  }}
    {{#collection.variant key="Basic Usage"}}
      <p>Most Favorite number: {{mostFavorite}}</p>
      {{#freestyle-usage "basic-bootstrap-select" name="Basic Select"}}
        {{bootstrap/simple-select
          label="Most Favorite Number"
          options=options
          value=mostFavorite
          onChange=(action (mut mostFavorite))
        }}
      {{/freestyle-usage}}
    {{/collection.variant}}
    {{#collection.variant key="Advanced Usage"}}
      <p>You are: {{data.value}}</p>
      {{#freestyle-usage "advanced-bootstrap-select" name="Advanced Select"}}
        {{#bootstrap/simple-select
          autofocus=data.autofocus
          bootstrapCustomClass=data.bootstrapCustomClass
          disabled=data.disabled
          errors=data.errors
          help=data.help
          multiple=data.multiple
          required=data.required
          srOnly=data.srOnly
          value=data.value
          label=data.label
          options=options
          onChange=(action (mut data.value))
          as |simpleSelect|
        }}
          {{simpleSelect.label}}
          {{#simpleSelect.select as |select|}}
            {{select.option}} Cool
          {{/simpleSelect.select}}
          {{simpleSelect.hint}}
        {{/bootstrap/simple-select}}
      {{/freestyle-usage}}
      {{freestyle-prop-types data=data propDefinitions=propDefinitions}}
    {{/collection.variant}}
    {{#collection.variant key="Custom Child Components"}}
      <p>Most Favorite number: {{mostFavorite}}</p>
      {{#freestyle-usage "child-components-bootstrap-simple-select" name="Custom Child Components"}}
        {{#bootstrap/simple-select
          label="How Cool Are You?"
          options=options
          value=mostFavorite
          help="Don't worry. This won't effect your social standing and is just for self reflection."
          onChange=(action (mut mostFavorite))
          as |simpleSelect|
        }}
          {{simpleSelect.label}}
          {{#simpleSelect.select as |selectOption index|}}
            {{#if (eq index 0)}}
              Least Cool
            {{else if selectOption.last}}
              Most Cool
            {{else}}
              {{selectOption.option}} Cool
            {{/if}}
          {{/simpleSelect.select}}
          {{simpleSelect.help}}
        {{/bootstrap/simple-select}}
      {{/freestyle-usage}}
      {{#freestyle-note "child-components-bootstrap-simple-select--notes"}}
        The simple-select a component yields 3 properties `label`, `select`, and `hint`.

        `{{label}}`
        This is the label for the select. It yields the text for the label if you need additional customization.

        `{{help}}`
        This is the help for the select. It yields the text for help if you need additional customization.

        `{{select}}`
        This is a component that represents the select and it has 1 child component `selectOption` and 1 property `index`.

        `{{selectOption.index}}`
        This is an integer that is the index of the current select option.

        `{{selectOption.first}}`
        This is a boolean the is true if this is the first option.

        `{{selectOption.last}}`
        This is a boolean the is true if this is the last option.

        `{{selectOption.option}}`
        This is the current option value.

      {{/freestyle-note}}
    {{/collection.variant}}
  {{/freestyle-collection}}
{{/section.subsection}}
