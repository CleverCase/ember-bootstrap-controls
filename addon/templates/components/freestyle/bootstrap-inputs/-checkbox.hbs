{{#section.subsection name='Checkbox Input'}}
  {{#freestyle-collection
    defaultKey='Basic Usage'
    inline=false
    as |collection|
  }}
    {{#collection.variant key='Basic Usage'}}
      <p>Checkbox Value: {{basicValue}}</p>
      {{#freestyle-usage 'basic-bootstrap-inputs-checkbox' name='Basic Checkbox Input'}}
        {{#bootstrap/form/-inputs as |inputs|}}
          {{inputs.checkbox
            label="Super Cool Checkbox"
            value=basicValue
          }}
        {{/bootstrap/form/-inputs}}
      {{/freestyle-usage}}
    {{/collection.variant}}
    {{#collection.variant key='Advanced Usage'}}
      {{#freestyle-usage 'advanced-bootstrap-inputs-checkbox' name='Advanced Checkbox Input'}}
        {{#bootstrap/form/-inputs as |inputs|}}
          {{#inputs.checkbox
            disabled=data.disabled
            label=data.label
            readonly=data.readonly
            required=data.required
            srOnly=data.srOnly
            tabIndex=data.tabIndex
            value=data.value
            onClick=(action 'simpleClick')
            as |checkbox|
          }}
            {{checkbox.input}}
            {{checkbox.label}}
          {{/inputs.checkbox}}
        {{/bootstrap/form/-inputs}}
      {{/freestyle-usage}}
      {{freestyle-prop-types data=data propDefinitions=propDefinitions}}
    {{/collection.variant}}
    {{#collection.variant key='Custom Child Components'}}
      {{#freestyle-usage 'checkbox-child-components' name='Checkbox Input'}}
        {{#bootstrap/form/-inputs as |inputs|}}
          {{#inputs.checkbox
            label="Super Cool Checkbox"
            value=basicValue
            as |checkbox|
          }}
            {{checkbox.input}}
            {{checkbox.label}}
          {{/inputs.checkbox}}
        {{/bootstrap/form/-inputs}}
      {{/freestyle-usage}}
      {{#freestyle-note "checkbox-child-components--notes"}}
        The checkbox component yields 2 child components `input` and `label`.

        `{{input}}`
        This is the checkbox input.

        `{{label}}`
        This is the label for the input. It yields the text for label if you need additional customization.
      {{/freestyle-note}}
    {{/collection.variant}}
  {{/freestyle-collection}}
{{/section.subsection}}
