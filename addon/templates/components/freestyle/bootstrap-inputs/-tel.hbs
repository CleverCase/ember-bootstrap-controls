{{#section.subsection name='Telephone Input'}}
  {{#freestyle-collection
    defaultKey='Basic Usage'
    inline=false
    as |collection|
  }}
    {{#collection.variant key='Basic Usage'}}
      {{#freestyle-usage 'basic-tel-input' name='Basic Telephone Input'}}
        {{#bootstrap/simple-form as |form|}}
          {{form.inputs.tel
            label="Phone #"
            value=basicValue
          }}
        {{/bootstrap/simple-form}}
      {{/freestyle-usage}}
    {{/collection.variant}}
    {{#collection.variant key='Advanced Usage'}}
      {{#freestyle-usage 'advanced-tel-input' name='Advanced Telephone Input'}}
        {{#bootstrap/simple-form as |form|}}
          {{form.inputs.tel
            autocomplete=data.autocomplete
            help=data.help
            disabled=data.disabled
            label=data.label
            maxlength=data.maxlength
            minlength=data.minlength
            placeholder=data.placeholder
            readonly=data.readonly
            required=data.required
            srOnly=data.srOnly
            tabIndex=data.tabIndex
            value=data.value
          }}
        {{/bootstrap/simple-form}}
      {{/freestyle-usage}}
      {{freestyle-prop-types data=data propDefinitions=propDefinitions}}
    {{/collection.variant}}
    {{#collection.variant key='Custom Child Components'}}
      {{#freestyle-usage 'tel-child-components' name='Telephone Input'}}
        {{#bootstrap/simple-form as |form|}}
          {{#form.inputs.tel
            label="Telephone"
            value=basicValue
            as |tel|
          }}
            {{tel.input}}
            {{tel.label}}
          {{/form.inputs.tel}}
        {{/bootstrap/simple-form}}
      {{/freestyle-usage}}
      {{#freestyle-note "password-child-components--notes"}}
        The checkbox component yields 2 child components `input` and `label`.

        `{{input}}`
        This is the input.

        `{{label}}`
        This is the label for the input. It yields the text for label if you need additional customization.
      {{/freestyle-note}}
    {{/collection.variant}}
  {{/freestyle-collection}}
{{/section.subsection}}
