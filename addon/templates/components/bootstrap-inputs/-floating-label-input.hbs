{{#bootstrap-inputs/-text
  classNames="form-label-group"
  label=label
  placeholder=label
  value=value
  help=help
  disabled=disabled
  maxlength=maxlength
  minlength=minlength
  readonly=readonly
  required=required
  tabIndex=tabIndex
  as |text|
}}
  {{text.input}}
  {{text.label}}
  {{#if help}}
    {{text.help}}
  {{/if}}
{{/bootstrap-inputs/-text}}
