{{#if hasBlock}}
  {{yield
    (hash
      label=(component 'bootstrap/-label'
        classNames='form-check-label'
        label=label
        srOnly=srOnly
        for=inputId
      )
      inputId=inputId
      input=(component 'bootstrap/-checkbox'
        checked=value
        elementId=inputId
        name=groupId
        type='radio'
        disabled=disabled
        readonly=readonly
        required=required
        srOnly=srOnly
        onClick=onClick
      )
    )
  }}
{{else}}
  {{bootstrap/-checkbox
    checked=value
    elementId=inputId
    name=groupId
    type='radio'
    disabled=disabled
    readonly=readonly
    required=required
    srOnly=srOnly
    onClick=onClick
  }}
  {{bootstrap/-label
    classNames='form-check-label'
    label=label
    srOnly=srOnly
    for=inputId
  }}
{{/if}}
