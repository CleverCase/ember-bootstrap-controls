{{#if enabled}}
  {{#if hasBlock}}
    {{yield (hash
      button=(component 'bootstrap/buttons/edit-button'
        classNames='btn-xs'
        aria-label="Edit Row"
        action=action
      )
    )}}
  {{else}}
    {{bootstrap/buttons/edit-button
      classNames='btn-xs'
      aria-label="Edit Row"
      action=action
    }}
  {{/if}}
{{/if}}
