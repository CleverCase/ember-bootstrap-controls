<input
  type="checkbox"
  id="{{control.inputId}}"
  checked={{isChecked}}
  onclick={{action "change" value="target.checked"}}
  name={{name}}
  disabled={{disabled}}
  required={{required}}
  autofocus={{autofocus}}
  tabindex={{tabindex}}>