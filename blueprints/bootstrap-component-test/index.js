/* eslint-env node */
const fs = require('fs');
module.exports = {
  description: '',

  // locals(options) {
  //   // Return custom template variables here.
  //   return {
  //     foo: options.entity.options.foo
  //   };
  // }

  beforeInstall(options) {
    // Remove other test.
  }
};
