import computerActionKey from '../../../utils/computer-action-key';
import { module, test } from 'qunit';

module('Unit | Utility | computer action key');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = computerActionKey();
  assert.ok(result);
});
