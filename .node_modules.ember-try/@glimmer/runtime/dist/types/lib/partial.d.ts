import { TemplateMeta } from '@glimmer/wire-format';
import { Template } from './template';
export declare class PartialDefinition<T extends TemplateMeta> {
    name: string;
    template: Template<T>;
    constructor(name: string, template: Template<T>);
}
