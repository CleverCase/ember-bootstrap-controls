import { Opaque } from '@glimmer/util';
import { Simple } from '@glimmer/interfaces';
export declare function normalizeProperty(element: Simple.Element, slotName: string): {
    normalized: any;
    type: any;
};
export declare function normalizePropertyValue(value: Opaque): Opaque;
