import { VersionedReference, Tag } from './validators';
export declare class ConstReference<T> implements VersionedReference<T> {
    protected inner: T;
    tag: Tag;
    constructor(inner: T);
    value(): T;
}
export declare function isConst(reference: {
    tag: Tag;
}): boolean;
